import path from 'path'

export const src = path.normalize(path.join(__dirname, "../src"))

export const build = path.normalize(path.join(__dirname, "../build"))

export const release = path.normalize(path.join(__dirname, "../release"))

export const releaseBuild = path.normalize(path.join(__dirname, "../release/build"))
